<p id="notice"><%= notice %></p>
<div class="card">
  <div class="card-header">
    Registra tu nuevo libro
  </div>
  <div class="card-body">
    <table class="table">
  <thead>
    <tr>
      <th>Título</th>
      <th>Category</th>
      <th>Year</th>
      <th colspan="3">Acciones</th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr class="<%= cycle('table-primary', '') %>">
        <td><%= book.title %></td>
        <td><%= book.category %></td>
        <td><%= book.year %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Seguro de eliminar?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
  </div>
  <div class="card-footer">
    <%= link_to 'New Book', new_book_path %>
  </div>
</div>
